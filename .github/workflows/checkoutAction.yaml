name: workflow for the action 
on: [push]
jobs:
    job_01:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              id: checkout_code
              uses: actions/hello-world-javascript-action@v1
              with:
                who-to-greet: 'Mona the Octocat'
            - name: provide o/p
              run: |
                    echo "Hello ${{ github.actor }}!"
                    echo "${{steps.checkout_code.outputs.time}}"

